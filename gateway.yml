zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
      sensitiveHeaders:
#    product: /product/**
  ignored-patterns:
  - /**/product/listForOrder
management:
  endpoints:
    web:
      exposure:
        include: routes,filters
